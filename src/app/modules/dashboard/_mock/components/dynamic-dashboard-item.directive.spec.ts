import { DynamicDashboardItemDirective } from './dynamic-dashboard-item.directive';

describe('DynamicDashboardItemDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicDashboardItemDirective();
    expect(directive).toBeTruthy();
  });
});
